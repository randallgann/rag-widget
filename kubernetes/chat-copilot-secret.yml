apiVersion: v1
kind: Secret
metadata:
  name: chat-copilot-secret
type: Opaque
stringData:
  # Add your environment variables from the .env file
  # This is just a placeholder - fill with actual values when deploying
  API_KEY: "your-api-key"
  # Add any other required env variables from your chat-copilot .env file